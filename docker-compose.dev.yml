services:
  motorsharks:
    build:
      context: .
      dockerfile: ServerNode/Dockerfile
    container_name: motorsharks-dev
    ports:
      - "8081:8080"
    environment:
      MOTORSHARKS_DATA_DIR: "/app/data"
    volumes:
      - ./data:/app/data
    restart: "no"
